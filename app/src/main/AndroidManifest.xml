<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ajisaputrars.moviecharts">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- SplashScreen -->
        <!--<activity-->
            <!--android:name=".SplashScreen"-->
            <!--android:label="@string/app_name" >-->
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->
                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        <!--</activity>-->

        <!-- MainActivity -->
        <activity
            android:name="com.example.ajisaputrars.moviecharts.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="android.app.searchable"
                android:resource="@xml/serchable"/>
        </activity>

        <!-- DetailActivity -->
        <activity
            android:name="com.example.ajisaputrars.moviecharts.DetailActivity"
            android:label="Movie Detail"
            android:parentActivityName="com.example.ajisaputrars.moviecharts.MainActivity" />

    </application>

</manifest>